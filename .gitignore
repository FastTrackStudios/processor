# Build artifacts
target/
result
result-*
.direnv/
.cache

# Node / vendored builds
node_modules/
dist/

# Databases / SQLite runtime files
*.db
*.db-journal
*.db-shm
*.db-wal
.dolt/

# Local-only env overrides (`.envrc` itself is tracked — see architect's
# pattern; `.envrc.local` is the per-machine escape hatch)
.envrc.local
.env

# Beads credentials
.beads/.beads-credential-key

# IDE / agent scratch
.claude/worktrees/
